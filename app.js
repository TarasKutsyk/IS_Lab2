const tablesService = require('./utility/tablesService');

const frequencyTable = tablesService.getFrequencyTable();

// tablesService.formSubstitutionTable(frequencyTable);
require('./encryption/encryptor');